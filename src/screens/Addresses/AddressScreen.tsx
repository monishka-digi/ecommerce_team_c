import React from 'react';
import {Text, View} from 'react-native';

const AddressesScreen = () => {
  return (
    <View>
      <Text>{'AddressesScreen Page'}</Text>
    </View>
  );
};

export default AddressesScreen;
